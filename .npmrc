# Ensure optional dependencies are installed
# This is required for Sharp to install platform-specific binaries on Vercel
# Sharp will automatically detect the platform and install the correct binaries

